# Telegram Bot для BazarPlus

Telegram бот с постоянными reply-кнопками для взаимодействия с пользователями.

## Функциональность

- 🌐 **Sayt haqida** - информация о сайте
- 🏪 **Sotuvchi bo'lish** - информация о том, как стать продавцом
- Постоянные reply-кнопки внизу клавиатуры
- Интеграция с базой данных Supabase для получения контента

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` в корне проекта и добавьте:
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
NEXT_PUBLIC_SITE_URL=https://your-site-url.com
```

## Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

## Запуск

### Разработка (с автоперезагрузкой):
```bash
npm run bot:dev
```

### Продакшн:
```bash
npm run bot
```

## Структура

- `bot/index.ts` - основной файл бота
- Использует библиотеку `telegraf` для работы с Telegram API
- Интегрирован с Supabase для получения данных из админ-панели

## Настройка контента

Контент для кнопок можно настроить в админ-панели:

1. **Welcome message** - в таблице `bot_settings` с ключом `welcome_message`
2. **Sayt haqida** - в таблице `site_settings` с ключом `site_about`
3. **Sotuvchi bo'lish** - в таблице `become_seller_page`

## Deploy

Для продакшн деплоя рекомендуется использовать:
- Heroku
- Railway
- VPS сервер
- Supabase Edge Functions

